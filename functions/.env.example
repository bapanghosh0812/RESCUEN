# RESCUEN Cloud Functions — secret reference (DO NOT put real values here).
#
# These are managed as Firebase secrets, not committed env vars. Set them with:
#   firebase functions:secrets:set GEMINI_API_KEY
#   firebase functions:secrets:set GMAIL_USER
#   firebase functions:secrets:set GMAIL_APP_PASSWORD
#
# This file only documents which secrets the functions expect.

GEMINI_API_KEY=
GMAIL_USER=
GMAIL_APP_PASSWORD=
